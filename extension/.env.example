# API base URL for the Syntive Cloudflare Worker (injected via __API_BASE__ at build time).
# wxt dev falls back to the default in wxt.config.ts when this is unset.
VITE_API_BASE=https://syntive.byztma.workers.dev